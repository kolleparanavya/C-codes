/* METHOD 1 */
#include<stdio.h>
#include<string.h>
int main(){
    char str1[101];
    scanf("%s",str1); /* getting input from user */
    int len = strlen(str1); /* for calculating string length */
    for(int i=0;i<len;i++){
        printf("%d\n",str1[i]);
    }
return 0;
}
